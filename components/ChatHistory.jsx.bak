import ReactMarkdown from 'react-markdown';

const ChatHistory = ({ messages, onClose }) => {
  return (
    <div className="absolute bottom-full mb-2 w-full bg-white rounded-xl border border-gray-200 shadow-lg z-10 max-h-[400px] overflow-y-auto">
      <div className="p-4 relative">
        <button
          onClick={onClose}
          className="absolute top-4 right-4 text-gray-400 hover:text-gray-600 transition-colors"
        >
          <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
        <div className="flex items-center px-3 pb-2">
          <span className="text-lg font-bold text-gray-800 mr-2">AI</span>
          <img src="/ol-logo.svg" alt="Logo" className="h-6" />
        </div>
        <div className="space-y-4">
          {messages.map((message, index) => (
            <div key={index} className={`flex ${message.sender === 'user' ? 'justify-end' : 'justify-start'}`}>
              <div
                className={`p-3 rounded-lg max-w-lg text-gray-800`}
              >
                {message.isLoading ? (
                  <div className="flex items-center">
                    <img src="/loader.gif" alt="Loading..." className="w-5 h-5 mr-2" />
                    <span>Thinking...</span>
                  </div>
                ) : (
                  <div className="prose"><ReactMarkdown>{message.text}</ReactMarkdown></div>
                )}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};

export default ChatHistory;
